export const log = (...args: any[]) => {
  console.log(...args)
}

/**
 * 返回 x + y 的值
 * @param x Number(x)
 * @param y Number(y)
 * @returns Number(x) + Number(y)
 */
export const add = (x: number, y: number) => Number(x) + Number(y)
